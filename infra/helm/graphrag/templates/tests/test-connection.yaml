apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "graphrag.index.fullname" . }}-test-connection"
  labels:
    {{- include "graphrag.index.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "graphrag.index.fullname" . }}:{{ .Values.index.service.port }}']
  restartPolicy: Never
